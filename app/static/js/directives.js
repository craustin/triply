'use strict';

/* Directives */


// angular.module('triplyApp.directives', []).
// 	directive('nappVersion', ['version', function(version) {
// 		return function(scope, elm, attrs) {
// 			elm.text(version);
// 		};
// 	}]);
angular.module('triplyApp.directives', []);
